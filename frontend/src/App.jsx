import React from 'react';

export default function App(){
  return (
    <div style={{fontFamily:'Arial, sans-serif', padding:20}}>
      <h1>ERMS - Frontend (placeholder)</h1>
      <p>Start implementing the React app (Vite / CRA / Next)</p>
    </div>
  );
}

